/**
 * Landing page layout — intentionally strips Navbar and Footer
 * This is a standalone funnel page for ad traffic
 */
export default function LandingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
